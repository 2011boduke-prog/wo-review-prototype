name: CI

on:
  push:
    branches: [main, dev]

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: Lint
        run: |
          flake8 backend nlp --max-line-length=100

      - name: Smoke test API
        run: |
          python backend/ingest_load.py
          python -m uvicorn backend.api:app --host 127.0.0.1 --port 8000 &
          sleep 5
          python - <<'PY'
          import urllib.request, json
          data = urllib.request.urlopen('http://127.0.0.1:8000/search?q=WO-1001').read()
          items = json.loads(data)
          assert isinstance(items, list)
          assert len(items) >= 1
          print('Smoke test passed with', len(items), 'result(s)')
          PY

  node:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 18
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: [python, node]
  #   steps:
  #     - name: Placeholder for Vercel/Render deployment
  #       run: echo "Add deployment steps here"
